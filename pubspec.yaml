name: tenor
description: Fetch, Search Tenor Gifs on dart client and server + on Flutter. Request for popular suggestions or categories amoung users around the world.
version: 2.0.0
homepage: https://github.com/justkawal/tenor

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  equatable: ^2.0.0

dev_dependencies:
  lints: ^2.0.1
